# 🚀 Инструкция по деплою на GitHub Pages

Пошаговое руководство по размещению вашего сайта в интернете бесплатно.

## 📋 Что нужно перед началом

1. **GitHub аккаунт** — если нет, создайте на [github.com](https://github.com)
2. **Git** — установите с [git-scm.com](https://git-scm.com/download/win) (если еще не установлен)

## 🔧 Шаг 1: Установка Git (если не установлен)

1. Скачайте Git для Windows: https://git-scm.com/download/win
2. Установите с настройками по умолчанию
3. Перезапустите терминал/командную строку

## 📦 Шаг 2: Подготовка проекта

### Вариант A: Через командную строку (рекомендуется)

1. **Откройте PowerShell или Git Bash** в папке проекта:
   - Правый клик на папке "Проект 2" → "Git Bash Here" (если установлен Git)
   - Или откройте PowerShell и перейдите в папку проекта

2. **Инициализируйте Git репозиторий:**
   ```bash
   git init
   ```

3. **Добавьте все файлы:**
   ```bash
   git add .
   ```

4. **Сделайте первый коммит:**
   ```bash
   git commit -m "Initial commit: Beauty Studio website"
   ```

### Вариант B: Через GitHub Desktop (проще для новичков)

1. Установите [GitHub Desktop](https://desktop.github.com/)
2. Войдите в свой GitHub аккаунт
3. File → Add Local Repository
4. Выберите папку "Проект 2"
5. Нажмите "Publish repository"

## 🌐 Шаг 3: Создание репозитория на GitHub

1. **Зайдите на [github.com](https://github.com)** и войдите в аккаунт
2. **Нажмите кнопку "+"** в правом верхнем углу → "New repository"
3. **Заполните форму:**
   - Repository name: `beauty-studio` (или любое другое имя)
   - Description: "Сайт для салона красоты с онлайн-записью"
   - Выберите **Public** (для бесплатного GitHub Pages)
   - **НЕ** ставьте галочки на "Add README" и других опциях
   - Нажмите **"Create repository"**

## 📤 Шаг 4: Загрузка кода на GitHub

### Если использовали командную строку (Шаг 2A):

1. **Подключите локальный репозиторий к GitHub:**
   ```bash
   git remote add origin https://github.com/ВАШ_USERNAME/beauty-studio.git
   ```
   (Замените `ВАШ_USERNAME` на ваш GitHub username)

2. **Переименуйте ветку в main:**
   ```bash
   git branch -M main
   ```

3. **Загрузите код:**
   ```bash
   git push -u origin main
   ```

4. **Введите логин и пароль** (или используйте Personal Access Token)

### Если использовали GitHub Desktop (Шаг 2B):

1. После добавления репозитория нажмите **"Publish repository"**
2. Введите имя репозитория
3. Нажмите **"Publish"**

## ⚙️ Шаг 5: Включение GitHub Pages

1. **Откройте ваш репозиторий** на GitHub
2. Перейдите в **Settings** (вкладка справа вверху)
3. Прокрутите вниз до раздела **"Pages"** (в левом меню)
4. В разделе **"Source"** выберите:
   - Branch: `main`
   - Folder: `/ (root)`
5. Нажмите **"Save"**

## ✅ Готово!

Через 1-2 минуты ваш сайт будет доступен по адресу:
```
https://ВАШ_USERNAME.github.io/beauty-studio/
```

(Замените `ВАШ_USERNAME` на ваш GitHub username и `beauty-studio` на имя репозитория)

## 🔄 Обновление сайта

После внесения изменений в код:

### Через командную строку:
```bash
git add .
git commit -m "Описание изменений"
git push
```

### Через GitHub Desktop:
1. Внесите изменения в файлы
2. В GitHub Desktop увидите изменения
3. Напишите описание коммита
4. Нажмите "Commit to main"
5. Нажмите "Push origin"

Сайт обновится автоматически через 1-2 минуты.

## 🔐 Personal Access Token (если требуется)

Если Git просит пароль, но обычный пароль не работает:

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token (classic)
3. Выберите срок действия и права: `repo` (все галочки)
4. Скопируйте токен
5. Используйте токен вместо пароля при `git push`

## ❓ Частые проблемы

### "git: command not found"
- Установите Git: https://git-scm.com/download/win
- Перезапустите терминал

### "Permission denied"
- Проверьте правильность username и репозитория
- Используйте Personal Access Token вместо пароля

### Сайт не открывается
- Подождите 2-3 минуты после включения Pages
- Проверьте, что выбрали branch `main` и folder `/ (root)`
- Убедитесь, что репозиторий Public

### Изменения не появляются
- Подождите 1-2 минуты (GitHub обновляет сайт не мгновенно)
- Очистите кэш браузера (Ctrl+F5)

## 📝 Дополнительно

### Подключение своего домена

1. В настройках Pages добавьте ваш домен
2. Настройте DNS записи у регистратора домена:
   - Тип: `CNAME`
   - Имя: `www` (или `@`)
   - Значение: `ВАШ_USERNAME.github.io`

### HTTPS

GitHub Pages автоматически предоставляет HTTPS для всех сайтов.

---

**Удачи с деплоем! 🎉**

Если возникнут вопросы, проверьте официальную документацию: https://docs.github.com/en/pages
